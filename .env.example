# Do not ever commit this real .env file to source control!
# PostgreSQL password (used by both Sigil and the database container)
POSTGRES_USER=sigil
POSTGRES_PASSWORD=sigil_dev_password
POSTGRES_DB=sigil

# Port to expose Sigil on (default: 8080)
# SIGIL_PORT=8080
